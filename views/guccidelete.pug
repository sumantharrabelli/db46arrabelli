extends layout 
 
block content 
  h1= title 
 
  if (toShow == null) 
    h2 No instance to delete 
  else 
            - let gucciid = toShow._id 
            - let Itemname = toShow.Itemname 
            - let quantity = toShow.quantity
            - let price = toShow.price 
            - let actionid = "/resource/gucci/" + gucciid 
    script let actionid = "#{actionid}" 
    div 
      div.row 
        div(class='gucciAttr mr-3') gucci ID is: 
        div #{gucciid} 
      div.row 
        div(class='gucciAttr mr-3') gucci Type is: 
        div #{Itemname} 
      div.row 
        div(class='gucciAttr mr-3') gucci quantity is: 
        div #{quantity} 
      div.row 
        div(class='gucciAttr mr-3') gucci Price is: 
        div #{price} 
 
    button(class="btn btn-danger m-2" onclick="deleteOnid()") Delete 
    button(class="btn btn-secondary m-2" onclick="cancel()") Cancel 
 
  script. 
    function cancel() { 
      // Go back to the home page for the costume collection 
      window.location.href='/gucci' 
    } 
  script. 
    function deleteOnid() { 
      console.log("starting delete") 
      // delete first s
      fetch(actionid, { 
        method: 'DELETE'       
      }).then((resp) => { 
        return resp.json();  
      }).then((body) => { 
        alert("Delete succeded") 
        window.location.href='/gucci' 
        // TODO handle body 
      }).catch((error) => { 
        // TODO handle error 
        alert("Delete failed " + error ) 
      }); 
    } 